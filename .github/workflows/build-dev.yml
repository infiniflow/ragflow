name: Build Multi-Arch Ragflow (development)

on:
  workflow_dispatch:
  push:
    branches: [ "development" ]

jobs:
  build-ragflow:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install Python deps for download_deps.py
        run: |
          python -m pip install --upgrade pip
          if [ -f requirements.txt ]; then
            pip install -r requirements.txt
          else
            pip install huggingface_hub nltk tiktoken requests
          fi

      - name: Run download_deps.py
        run: |
          python download_deps.py

      - name: Set up QEMU (amd64 + arm64)
        uses: docker/setup-qemu-action@v3
        with:
          platforms: amd64,arm64

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      # Build deps as multi-arch and PUSH (cannot --load multi-arch)
      - name: Build & Push ragflow_deps (multi-arch)
        run: |
          docker buildx build \
            --platform linux/amd64,linux/arm64 \
            -f Dockerfile.deps \
            -t eurodoc/ragflow_deps:latest \
            --push .

      # Build final image as multi-arch and push ONE generic tag
      - name: Build & Push Ragflow (multi-arch)
        run: |
          docker buildx build \
            --platform linux/amd64,linux/arm64 \
            -f Dockerfile \
            -t eurodoc/medlibre-ragflow:dev \
            --push .
