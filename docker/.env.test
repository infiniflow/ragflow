# RAGFlow Test Environment Configuration
# 
# This file demonstrates how to configure RAGFlow for testing purposes.
# Copy this file to `.env` and set actual values for your test environment.
#
# For local unit tests that don't require a running server, no configuration is needed.
# This file is for integration and API contract tests.

# ============================================================================
# INTEGRATION AND API CONTRACT TESTS
# ============================================================================

# Required for integration and API contract tests
# Get your API key from: https://open.bigmodel.cn/
# Comment out to skip integration/contract tests
# ZHIPU_AI_API_KEY=your-zhipu-ai-api-key-here

# RAGFlow server address (optional, defaults to local)
# HOST_ADDRESS=http://127.0.0.1:9380

# ============================================================================
# TEST ENVIRONMENT SETUP
# ============================================================================

# Set this to 'true' when running tests inside Docker container
# DOCKER_CONTAINER=true

# Database type for integration tests (optional)
# DATABASE_TYPE=postgres

# ============================================================================
# STANDARD RAGFLOW CONFIGURATION (if running via Docker)
# ============================================================================

# See the main .env file for full documentation on these settings.
# Only essential settings for testing are shown below:

# Vector DB type (optional for tests)
# DOC_ENGINE=elasticsearch

# Device for inference
# DEVICE=cpu

# MinIO configuration (if testing document uploads)
# MINIO_HOST=minio:9000
# MINIO_USER=minioadmin
# MINIO_PASSWORD=minioadmin

# MySQL configuration
# MYSQL_HOST=mysql
# MYSQL_PORT=3306
# MYSQL_USER=root
# MYSQL_PASSWORD=infini_rag_flow

# Redis configuration
# REDIS_HOST=redis
# REDIS_PORT=6379

# ============================================================================
# NOTES ON TEST TYPES
# ============================================================================
#
# UNIT TESTS (no configuration needed):
#   - These tests are completely self-contained
#   - Run with: python run_tests.py --unit
#   - Examples: time utility tests, string parsing, token counting
#
# INTEGRATION TESTS (requires ZHIPU_AI_API_KEY):
#   - Test full workflows (upload → parse → retrieve → chat)
#   - Require a running RAGFlow server
#   - Require valid API credentials
#   - Run with: python run_tests.py --integration
#   - Will skip gracefully if ZHIPU_AI_API_KEY is not set
#
# API CONTRACT TESTS (requires ZHIPU_AI_API_KEY):
#   - Validate HTTP/SDK/Web API interfaces
#   - Require a running RAGFlow server
#   - Run with: python run_tests.py --contract
#   - Will skip gracefully if ZHIPU_AI_API_KEY is not set
#
# ALL TESTS:
#   - Run with: python run_tests.py --all
#   - Unit tests always run
#   - Integration and contract tests skip if ZHIPU_AI_API_KEY is missing
#
# QUICK START FOR LOCAL TESTING:
#   1. For unit tests only:
#      python run_tests.py --unit
#      (no setup needed)
#
#   2. For integration tests:
#      a. Get ZHIPU_AI_API_KEY from https://open.bigmodel.cn/
#      b. Start RAGFlow: docker-compose up -d
#      c. Set ZHIPU_AI_API_KEY in this file or environment
#      d. Run: python run_tests.py --integration
#
