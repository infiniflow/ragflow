user  root;
worker_processes  auto;

# Logs go to the container's stdout/stderr so `docker logs` and `kubectl logs`
# show them, and so nginx needs no writable path under /var/log.
error_log  /dev/stderr notice;
# Everything nginx writes lives under /ragflow/nginx, which is owned by group 0
# and group-writable, so it also works when the container runs as an arbitrary
# UID (OpenShift restricted-v2).
pid        /ragflow/nginx/nginx.pid;


events {
    worker_connections  1024;
}


http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;

    log_format  main  '$remote_addr - $remote_user [$time_local] "$request" '
                      '$status $body_bytes_sent "$http_referer" '
                      '"$http_user_agent" "$http_x_forwarded_for"';

    access_log  /dev/stdout  main;

    client_body_temp_path  /ragflow/nginx/tmp/client_body;
    proxy_temp_path        /ragflow/nginx/tmp/proxy;
    fastcgi_temp_path      /ragflow/nginx/tmp/fastcgi;
    uwsgi_temp_path        /ragflow/nginx/tmp/uwsgi;
    scgi_temp_path         /ragflow/nginx/tmp/scgi;

    sendfile        on;
    #tcp_nopush     on;

    keepalive_timeout  65;

    #gzip  on;
    client_max_body_size 1024M;

    include /ragflow/nginx/conf.d/ragflow.conf;
}
